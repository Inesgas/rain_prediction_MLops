apiVersion: v1
kind: Service
metadata:
  name: rain-prediction-airflow
  namespace: rain-prediction
spec:
  type: ClusterIP
  selector:
    app: rain-prediction-airflow
    component: webserver
  ports:
    - name: web
      port: 8080
      targetPort: 8080
