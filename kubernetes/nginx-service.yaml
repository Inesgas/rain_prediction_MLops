apiVersion: v1
kind: Service
metadata:
  name: nginx-gateway
  namespace: rain-prediction
spec:
  type: LoadBalancer
  selector:
    app: nginx-gateway
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
